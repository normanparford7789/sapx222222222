# Virtual Cam — Access Codes
# Add one code per line. Delete a line to revoke that code immediately.
# Lines starting with # are ignored... but since the app does exact line matching,
# keep comments only at the top and keep code lines clean (no spaces).
uiiihuiuuyuyli
gg
